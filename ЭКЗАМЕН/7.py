# import random                                       # import модуль (random)
#
# a = int(input("введите длину требуемого списка "))  # вводим целое число которое будет длиной списка
# list = [random.randint(1, 50) for a in  range(a)]   # генерируем случайный список , задаём диапазон от (1,50) , функция  range(a) задаём колличество элементов в списке
# print(list)